import json,re

# Common Methods:

# json.dump(obj, file): Writes a Python object as JSON to a file.
print(json.dumps({"name": "John", "age": 30}))


# json.dumps(obj): Converts a Python object to a JSON string.


# json.loads(string): Parses a JSON string and returns a Python object.


# json.load(file): Reads JSON data from a file and returns a Python object.



# 8. re (Regular Expressions)
# Provides support for working with regular expressions.

# Common Methods:

# re.match(pattern, string): Matches a pattern at the start of the string.


# re.search(pattern, string): Searches the string for the first match of the pattern.


# re.findall(pattern, string): Returns a list of all matches of the pattern in the string.


# re.sub(pattern, repl, string): Replaces matches of the pattern in the string with repl.


# re.compile(pattern): Compiles a regular expression pattern for reuse.
