'''
Common Methods:



'''
import sys
print("--------------")
# sys.argv: A list of command-line arguments passed to a Python script. it return Path of running file  code
print(sys.argv)
print("--------------")

# sys.exit([status]): Exits the script with an optional status code.
# print(sys.exit(0))
print("--------------")

# sys.version: Returns the Python interpreter's version information.
print(sys.version)
print("--------------")

# sys.path: A list of directories that the interpreter searches for modules., this current code uses the current directory that path is it return as list
print(sys.path)
print("--------------")

# sys.platform: Returns the platform name (e.g., 'win32', 'linux').
print(sys.platform)
print("--------------")